# Force text files to be UTF-8 with LF line endings
* text=auto eol=lf

# Explicitly mark documentation files as UTF-8
*.md text eol=lf
*.txt text eol=lf

# Explicitly mark YAML files as UTF-8
*.yaml text eol=lf
*.yml text eol=lf

# CSV (optional)
*.csv text eol=lf
